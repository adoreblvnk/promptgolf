# PromptGolf landing redesign — M2 read-only Judge

**Reviewed:** 2026-07-14 (SGT)
**Mode:** OpenCode `opencode run --pure`, attachments only, no auto-approval, read-only review.
**Inputs:** research passes 01–02, direction selection, and all three desktop rendered studies.

## Initial verdict

**APPROVE WITH FIXES**

### P0

None.

### P1 findings

1. Decision-matrix totals were arithmetically wrong. Correct values are A 385, B 485, C 378; Direction B still wins decisively.
2. “Different human” contradicted the controlled variable, which is specification quality.
3. The initial selected study stated more than it demonstrated: scores changed, but prompt/spec excerpts and the fact that all three generated apps remain visibly plausible were not shown together.
4. “96 VERIFIED SCORE” was unsupported by the supplied evidence; the durable seeded facts are hidden survival of 3/10, 7/10, and 10/10.

### Material P2 findings

- Production still needs semantic tabs, keyboard behavior, focus treatment, and state announcements.
- Small subdued mono annotations could become hard to read, especially on mobile.
- The isolated CTA and empty upper-right field weakened first-viewport density; held-constant conditions should appear beside the thesis.
- Direction B uses familiar dark developer-tool vocabulary, but the evidence trace and held-constant experiment keep it below category-cosplay threshold.
- The semantic DOM/CSS + limited SVG media decision is credible and materially safer than video, frame sequences, canvas, or WebGL.

## Resolution applied

- Corrected weighted totals to **385 / 485 / 378**.
- Replaced the headline with **“Same agent. Same task. Different specification.”**
- Replaced the unsupported 96 with **10/10 hidden survival**.
- Added a first-viewport held-constant block naming the checkout, OpenAI builder, and stored contract + Playwright evaluator.
- Added short naive, structured, and expert prompt/spec excerpts.
- Added an explicit constant that the visible checkout renders in all three states while the hidden contract separates them.
- Increased key mono annotation sizes in the selected study.
- Strengthened implementation guardrails for equivalent generated-product evidence, semantic controls, keyboard/focus, reduced motion, contrast, and mobile overflow.

## Post-fix disposition

All P1 findings were addressed in the M2 evidence artifact. Material P2 items that require working interaction remain explicit M3/M4 acceptance gates. **Direction B — The Human-Spec Gap remains selected and is cleared for bounded implementation.**
